source 'https://rubygems.org'

# Contact + subscribe backend served on Render (see render.yaml). The
# old Gemfile used to be Jekyll-only and was removed in the Astro
# cutover; without it, `bundle install` fails on Render and every deploy
# has been silently frozen on the last pre-cutover revision.

ruby '>= 3.1'

gem 'sinatra',   '~> 4.1'
gem 'puma',      '~> 6.4'
gem 'rack-cors', '~> 2.0'
gem 'mailtrap',  '~> 2.2'
